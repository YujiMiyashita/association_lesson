<%= form_for(@my_number) do |f| %>
  <% if @my_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_number.errors.count, "error") %> prohibited this my_number from being saved:</h2>

      <ul>
      <% @my_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
